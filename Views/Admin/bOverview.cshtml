@model dynamic
@{
    ViewData["Title"] = "Index Orders";
}

<h5 class="text-center m-2 p-2">Overzicht bestellingen per product</h5>

<table>
    <thead>
        <tr>
            <th class="align-middle">Product</th>
            <th class="text-center align-middle">Aantal</th>
        </tr>
    </thead>
    @{
        decimal total = 0;
    }
    <tbody>
        @for (var i = 0; i < Model.Details.Count; i++)
        {

            <tr>
                <td class="align-middle">
                    @{
                        total += Model.Details[i].Quantity * Model.Details[i].Price;
                    }
                    @Model.Details[i].Productname
                </td>
                <td class="text-center align-middle">

                    @Model.Details[i].Quantity
                </td>
            </tr>
        }
    </tbody>
    <tfoot>
        <tr>
            <td>
                <b>TOTAALPRIJS: </b>€ @total
            </td>
        </tr>
    </tfoot>

</table>
